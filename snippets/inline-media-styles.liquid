<style>
  .inline-media {
    margin: 2rem 0;
    width: 100%;
    --inline-media-scale: 1;
  }

  /* Images with border around entire figure */
  .inline-media--image {
    border: 2px solid #000;
    padding: 12px 12px 12px 12px;
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
    background: #d9d9d9;
    margin: 0;
  }

  .inline-media--image img {
    width: 100%;
    height: auto;
    display: block;
    border: none;
    margin: 0;
    padding: 0;
    vertical-align: bottom;
  }

  /* Image captions - highest specificity to override any other styles */
  .inline-media--image figcaption,
  figure.inline-media--image figcaption,
  .article-content .inline-media--image figcaption,
  .article-content figure.inline-media--image figcaption,
  .blog-post-content .inline-media--image figcaption,
  .blog-post-content figure.inline-media--image figcaption {
    font-size: calc(1rem * var(--inline-media-scale, 1)) !important;
    font-family: 'Times New Roman', 'Times', serif !important;
    color: #000 !important;
    margin: 0 !important;
    padding: 12px 12px 0 12px !important;
    text-align: center !important;
    font-style: normal !important;
    line-height: 1.4 !important;
    background: transparent !important;
  }

  .inline-media video {
    width: 100%;
    height: auto;
    border: 2px solid rgba(0, 0, 0, 0.15);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  .inline-media audio {
    width: 100%;
    margin-top: 0.5rem;
  }

  /* Video embeds - always centered, no layout options */
  .inline-media--embed {
    width: 100%;
    max-width: 800px;
    margin: 2rem auto;
    clear: both;
  }

  .inline-media--embed iframe {
    width: 100%;
    aspect-ratio: 16 / 9;
    border: 2px solid rgba(0, 0, 0, 0.15);
    box-shadow: 0 2px 8px rgba(0, 0, 0, 0.08);
  }

  /* General figcaption for non-image media (video, audio, embed) */
  .inline-media--embed figcaption,
  .inline-media--video figcaption,
  .inline-media--audio figcaption {
    font-size: 0.75rem;
    font-family: 'Georgia', serif;
    color: #000;
    margin-top: 0.75rem;
    text-align: center;
    font-style: normal;
    line-height: 1.4;
    padding: 0 0.5rem;
  }

  /* Full-width layout (default) */
  .inline-media--full {
    width: calc(80% * var(--inline-media-scale, 1));
    max-width: calc(800px * var(--inline-media-scale, 1));
    margin: 2rem auto;
    clear: both;
  }

  /* Float right layout - text wraps on left */
  .inline-media--float-right {
    float: right;
    width: calc(45% * var(--inline-media-scale, 1));
    max-width: calc(500px * var(--inline-media-scale, 1));
    margin: 0 0 1.5rem 2rem;
    clear: right;
  }

  /* Float left layout - text wraps on right */
  .inline-media--float-left {
    float: left;
    width: calc(45% * var(--inline-media-scale, 1));
    max-width: calc(500px * var(--inline-media-scale, 1));
    margin: 0 2rem 1.5rem 0;
    clear: left;
  }

  /* Center layout - centered with limited width */
  .inline-media--center {
    width: calc(50% * var(--inline-media-scale, 1));
    max-width: calc(500px * var(--inline-media-scale, 1));
    margin: 2rem auto;
    clear: both;
  }

  /* Responsive adjustments */
  @media screen and (max-width: 749px) {
    .inline-media {
      margin: 1.5rem 0;
    }

    /* Reduce padding on mobile */
    .inline-media--image {
      padding: 0 0 8px 0;
    }

    /* Mobile image captions - highest specificity */
    .inline-media--image figcaption,
    figure.inline-media--image figcaption,
    .article-content .inline-media--image figcaption,
    .article-content figure.inline-media--image figcaption,
    .blog-post-content .inline-media--image figcaption,
    .blog-post-content figure.inline-media--image figcaption {
      padding: 8px 8px 0 8px !important;
      font-size: calc(0.5rem * var(--inline-media-scale, 1)) !important;
      font-family: 'Times New Roman', 'Times', serif !important;
    }

    /* On mobile, all layouts become full-width */
    .inline-media--float-right,
    .inline-media--float-left,
    .inline-media--center {
      float: none;
      width: min(100%, calc(100% * var(--inline-media-scale, 1)));
      max-width: 100%;
      margin: 1.5rem 0;
    }
  }

  /* Clear floats after content */
  .article-content::after,
  .blog-post-content::after,
  .rte::after {
    content: '';
    display: table;
    clear: both;
  }
</style>